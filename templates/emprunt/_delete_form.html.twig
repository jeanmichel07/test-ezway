<form class="pt-2" method="post" action="{{ path('supprimer_emprunt', {'id': emprunt.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ emprunt.id) }}">
    <button class="btn btn-outline-danger">Supprimer</button>
</form>
