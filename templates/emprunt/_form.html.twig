{{ form_start(form) }}
    <div class="form-group">
        {{ form_row(form.livre,{'attr':{'class':'form-select'}}) }}
    </div>
    <div class="form-group">
        {{ form_row(form.abonne,{'attr':{'class':'form-select'}}) }}
    </div>
    <button class="btn btn-primary mt-2">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
