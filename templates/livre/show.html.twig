{% extends 'base.html.twig' %}

{% block title %}Livre{% endblock %}

{% block body %}
    <h1>Livre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ livre.id }}</td>
            </tr>
            <tr>
                <th>Auteur</th>
                <td>{{ livre.auteur }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ livre.titre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('livre') }}" class="btn btn-outline-primary">Retour a la liste</a>

    <a href="{{ path('modifier_livre', {'id': livre.id}) }}" class="btn btn-secondary">Modifier</a>

    {{ include('livre/_delete_form.html.twig') }}
{% endblock %}
