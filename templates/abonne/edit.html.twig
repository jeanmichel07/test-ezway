{% extends 'base.html.twig' %}

{% block title %}Edit Abonne{% endblock %}

{% block body %}
    <h1>Modification</h1>

    {{ include('abonne/_form.html.twig', {'button_label': 'Modifer'}) }}

    <a href="{{ path('abonne') }}" class="btn btn-outline-primary">Retour à la liste</a>

    {{ include('abonne/_delete_form.html.twig') }}
{% endblock %}
