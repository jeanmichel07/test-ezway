{% extends 'base.html.twig' %}

{% block title %}Abonne{% endblock %}

{% block body %}
    <h1>Abonne</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ abonne.id }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ abonne.prenom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('abonne') }}" class="btn btn-outline-primary">Retour à la liste</a>

    <a href="{{ path('modifier_abonne', {'id': abonne.id}) }}" class="btn btn-secondary">Modifier</a>

    {{ include('abonne/_delete_form.html.twig') }}
{% endblock %}
